<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="apliksi.apikuan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.TransNav">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".Registerdua" />
        <activity android:name=".Home" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Catatan_imunisasi" />
        <activity android:name=".Jadwal_imunisasi" />
        <activity android:name=".Lihat" />
        <activity android:name=".Kebutuhan_gizi" />
        <activity
            android:name=".Navigasi_derawer"
            android:label="@string/title_activity_navigasi_derawer"
            android:theme="@style/AppTheme.TransNav" />
        <activity android:name=".Card_aktivity"></activity>
    </application>

</manifest>